# LLM Analysis (Article Intelligence V2 — deepseek-v4-flash via OpenRouter)
MODEL=deepseek/deepseek-v4-flash
# OpenRouter API key (sk-or-...). Get one at https://openrouter.ai/keys
API_KEY=
LLM_BASE=https://openrouter.ai/api/v1

# Shared inference pool (optional). Start once:
#   pm2 start .venv/bin/python --name alpha-pool -- -m neurons.inference_pool
# Then set this on each thin miner so they skip loading ~9GB NER stacks:
INFERENCE_POOL_URL=http://127.0.0.1:30000
# Pool process tuning (read by alpha-pool only):
# INFERENCE_POOL_HOST=127.0.0.1
# INFERENCE_POOL_PORT=30000
# INFERENCE_POOL_WORKERS=4
# INFERENCE_POOL_TIMEOUT=600

# X/Twitter API Configuration
X_BEARER_TOKEN=
X_API_BASE=https://api.twitter.com/2

# Miner API Configuration (API v2)
# Base URL for the miner API (API v2 endpoint: /v2/submit)
MINER_API_URL=https://api.alpharidge.ai
# HTTP timeout in seconds for API requests
BATCH_HTTP_TIMEOUT=30.0

# Block-based Rate Limiting Configuration
# Number of blocks per rate limit window (default: 100 blocks, ~20 minutes at 12s per block)
BLOCKS_PER_WINDOW=100
# Maximum posts to scrape and submit per block window (default: 5, matches API v2 rate limit)
# Must match MAX_SUBMISSION_RATE in api_v2 if using API v2
MAX_SUBMISSIONS_PER_WINDOW=5
